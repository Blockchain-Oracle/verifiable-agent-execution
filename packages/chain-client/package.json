{
  "name": "@verifiable-agent-execution/chain-client",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "description": "ethers v6 wrapper over the pre-deployed AgenticID (ERC-7857 example) on 0G Galileo, plus the SessionAnchor that orchestrates flush + mint at session end.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@verifiable-agent-execution/logger": "workspace:*",
    "ethers": "~6.13.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "@vitest/coverage-v8": "^2.0.0",
    "typescript": "^5.5.4",
    "vitest": "^2.0.0"
  }
}
