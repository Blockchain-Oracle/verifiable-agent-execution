{
  "name": "verifiable-agent-execution",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "description": "Cryptographic receipts for every AI agent session. 0G APAC Hackathon 2026.",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "tsc --noEmit",
    "dev": "pnpm --filter web dev"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.2",
    "@types/node": "^20.14.10",
    "typescript": "^5.5.4"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
